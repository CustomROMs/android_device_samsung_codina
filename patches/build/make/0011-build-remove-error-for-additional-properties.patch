From 30fa572455932ba072d8edf50cef920d8454999f Mon Sep 17 00:00:00 2001
From: mydongistiny <jaysonedson@gmail.com>
Date: Mon, 21 Aug 2017 19:34:34 -0700
Subject: [PATCH 11/32] build: remove error for additional properties

Change-Id: I42ade0ea75dae2a5b819921e0a551e3ffc85392e
---
 core/Makefile |  2 +-
 core/main.mk  | 16 ----------------
 2 files changed, 1 insertion(+), 17 deletions(-)

diff --git a/core/Makefile b/core/Makefile
index df65bb09a..96c4ce379 100644
--- a/core/Makefile
+++ b/core/Makefile
@@ -15,7 +15,7 @@ LOCAL_PATH := $(BUILD_SYSTEM)
 #$(2): the dest
 define check-product-copy-files
 $(if $(filter-out $(TARGET_COPY_OUT_SYSTEM_OTHER)/%,$(2)), \
-  $(if $(filter %.apk, $(2)),$(error \
+  $(if $(filter %.apk, $(2)),$(warning \
      Prebuilt apk found in PRODUCT_COPY_FILES: $(1), use BUILD_PREBUILT instead!)))
 endef
 # filter out the duplicate <source file>:<dest file> pairs.
diff --git a/core/main.mk b/core/main.mk
index 7a9d3a355..2e229764d 100644
--- a/core/main.mk
+++ b/core/main.mk
@@ -132,22 +132,6 @@ endif
 
 #
 # -----------------------------------------------------------------
-# Validate ADDITIONAL_DEFAULT_PROPERTIES.
-ifneq ($(ADDITIONAL_DEFAULT_PROPERTIES),)
-$(error ADDITIONAL_DEFAULT_PROPERTIES must not be set before here: $(ADDITIONAL_DEFAULT_PROPERTIES))
-endif
-
-#
-# -----------------------------------------------------------------
-# Validate ADDITIONAL_BUILD_PROPERTIES.
-ifneq ($(ADDITIONAL_BUILD_PROPERTIES),)
-$(error ADDITIONAL_BUILD_PROPERTIES must not be set before here: $(ADDITIONAL_BUILD_PROPERTIES))
-endif
-
-ADDITIONAL_BUILD_PROPERTIES :=
-
-#
-# -----------------------------------------------------------------
 # Add the product-defined properties to the build properties.
 ifdef PRODUCT_SHIPPING_API_LEVEL
 ADDITIONAL_BUILD_PROPERTIES += \
-- 
2.11.0

