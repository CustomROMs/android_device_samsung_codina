From b80971925d1b767073438ea837e6c578481faa51 Mon Sep 17 00:00:00 2001
From: Shilin Victor <chrono.monochrome@gmail.com>
Date: Fri, 13 Jan 2017 13:33:48 +0700
Subject: [PATCH 14/14] Fix build

Change-Id: Ie73a5e331407254b63dc93545612053b979962a0
---
 src/com/android/settings/rr/input/HWSettings.java | 15 ++++++++-------
 1 file changed, 8 insertions(+), 7 deletions(-)

diff --git a/src/com/android/settings/rr/input/HWSettings.java b/src/com/android/settings/rr/input/HWSettings.java
index 7e16fa6..b202909 100644
--- a/src/com/android/settings/rr/input/HWSettings.java
+++ b/src/com/android/settings/rr/input/HWSettings.java
@@ -180,9 +180,9 @@ public class HWSettings extends ActionFragment implements
         int keysDisabled = 0;
         if (!needsNavbar) {
             mHwKeyDisable = (SwitchPreference) findPreference(HWKEY_DISABLE);
-            keysDisabled = Settings.Secure.getIntForUser(getContentResolver(),
+            keysDisabled = 0; /*Settings.Secure.getIntForUser(getContentResolver(),
                     Settings.Secure.HARDWARE_KEYS_DISABLE, 0,
-                    UserHandle.USER_CURRENT);
+                    UserHandle.USER_CURRENT);*/
             mHwKeyDisable.setChecked(keysDisabled != 0);
             mHwKeyDisable.setOnPreferenceChangeListener(this);
         } else {
@@ -300,15 +300,15 @@ public class HWSettings extends ActionFragment implements
 
     private void updateDisableNavkeysCategories() {
         final PreferenceScreen prefScreen = getPreferenceScreen();
-	    boolean enabled = Settings.Secure.getInt(getActivity().getContentResolver(),
-                Settings.Secure.HARDWARE_KEYS_DISABLE, 0) == 1;
+	    boolean enabled = false;/*Settings.Secure.getInt(getActivity().getContentResolver(),
+                Settings.Secure.HARDWARE_KEYS_DISABLE, 0) == 1;*/
         final ButtonBacklightBrightness backlight =
                 (ButtonBacklightBrightness) prefScreen.findPreference(KEY_BUTTON_BACKLIGHT);
         final SharedPreferences prefs = PreferenceManager.getDefaultSharedPreferences(getContext());
         final int defaultBrightness = getContext().getResources().getInteger(com.android.internal.R.integer.config_buttonBrightnessSettingDefault);
-        int keysDisabled = Settings.Secure.getIntForUser(getContentResolver(),
+        int keysDisabled = 0; /*Settings.Secure.getIntForUser(getContentResolver(),
                     Settings.Secure.HARDWARE_KEYS_DISABLE, 0,
-                    UserHandle.USER_CURRENT);
+                    UserHandle.USER_CURRENT);*/
  	    /* Save/restore button timeouts to disable them in softkey mode */
         if (keysDisabled == 1) {
             CMSettings.Secure.putInt(getContext().getContentResolver(),
@@ -342,8 +342,9 @@ public class HWSettings extends ActionFragment implements
     public boolean onPreferenceChange(Preference preference, Object newValue) {
          if (preference == mHwKeyDisable) {
             boolean value = (Boolean) newValue;
+/*
             Settings.Secure.putInt(getContentResolver(), Settings.Secure.HARDWARE_KEYS_DISABLE,
-                    value ? 1 : 0);
+                    value ? 1 : 0); */
             setActionPreferencesEnabled(!value);
             updateDisableNavkeysCategories();
             return true;
-- 
2.9.3

